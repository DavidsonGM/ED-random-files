#include <stdio.h>

//definindo estrutura
struct endereco {
  char nome[50], rua[40];
  int cep, numero;
};

int main() {

  //declaracao de variaveis
  char nome[50], rua[40];
  int cep, numero;

  //declaracao da variavel e que eh do tipo endereco
  struct endereco e;

  return(0);
}