#include <stdio.h>

typedef struct {
    int x;
    int y;
} Coordenada;

Coordenada* aloca_vetor_coordenadas(unsigned int tamanho);

Coordenada* aloca_vetor_coordenadas(unsigned int tamanho){
    Coordenada *ptr = malloc(tamannho);


}
int main(){
    Coordenada* aloca_vetor_coordenadas(sizeof(tamanho));
}
